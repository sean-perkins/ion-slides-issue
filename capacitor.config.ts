import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-slides-router-outlet',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
